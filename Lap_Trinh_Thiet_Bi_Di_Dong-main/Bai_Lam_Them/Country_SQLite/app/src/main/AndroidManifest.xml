<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nguyenvanhailong.country_sqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Country_SQLite">
        <activity android:name=".AddCountryActivity"
            android:label="Thêm sách"
            android:parentActivityName=".CountryListActivity"/>
        <activity
            android:name=".ModifyCountryActivity"
            android:label="Cập nhật sách"
            android:parentActivityName=".CountryListActivity" />
        <activity
            android:name=".CountryListActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>